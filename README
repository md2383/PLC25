Directions on building the project in a Linux environment on the cs machines.


1) Navigate to the PLC25 directory: cd $/(path)/(to)/PLC25

2) Compile the Java files. Make sure you include the [ -d . ] option to specify 
    the current directory as the root for the package structure:

    > [ javac -d . Root/provided/*.java Root/jott_interpreter/nodes/*.java Root/jott_interpreter/nodes/token_nodes/*.java Root/jott_interpreter/nodes/grammar_nodes/*.java Root/jott_interpreter/nodes/function_nodes/*.java Root/jott_interpreter/*.java Root/testers/*.java ]

3) To run the JottTokenizerTester class, you need to specify the correct package name,
    and to specify the classpath explicitly.

    > [ java -cp . testers.JottTokenizerTester ]

4) To run the JottParserTester class, you need to specify the correct package name,
    and to specify the classpath explicitly.

    > [ java -cp . testers.JottParserTester ]